## Prerequisites

- Node.js 18.x or higher
- npm 9.x or higher
- Playwright 1.52.0 or higher

## Installation

1. Clone the repository
2. Run `npm ci` to install dependencies
3. Run `npx playwright install --with-deps chromium` to install Playwright browsers

## Running Tests
npx playwright test --project=FE-Login